// ### **Exercício 1 – Verificação de valor**

// Escreva uma função `checkValue(value)` que:

// * Imprime `"Truthy"` se o valor for truthy.
// * Imprime `"Falsy"` se o valor for falsy.

// **Exemplo de entrada:**

// ```js
// checkValue(0);       // Falsy
// checkValue("hello"); // Truthy
// ```

// ---

// ### **Exercício 2 – Checagem de senha**

// Crie uma função que receba uma senha.

// * Se a senha for uma string vazia, `null` ou `undefined`, imprima `"Senha inválida"`.
// * Caso contrário, imprima `"Senha aceita"`.

// ---

// ### **Exercício 3 – Verificando número**

// Dado um número, use `if` para:

// * Imprimir `"Número válido"` se o número for truthy.
// * Imprimir `"Número inválido"` se for falsy (`0`, `NaN`, `null`, `undefined`).

// Teste com: `0`, `10`, `NaN`.

// ---

// ### **Exercício 4 – Preferência de usuário**

// Crie uma função que receba um valor de preferência de tema (`darkMode`).

// * Se for truthy, imprima `"Modo escuro ativado"`.
// * Se for falsy, imprima `"Modo claro ativado"`.

// Exemplo:

// ```js
// setTheme(true);   // Modo escuro ativado
// setTheme(null);   // Modo claro ativado
// ```

// ---

// ### **Exercício 5 – Verificando lista**

// Receba um array.

// * Se o array for vazio (`[]`) ou falsy, imprima `"Sem itens"`.
// * Caso tenha elementos, imprima `"Lista possui itens"`.

// **Exemplo de entrada:**

// ```js
// checkList([]);          // Sem itens
// checkList([1,2,3]);     // Lista possui itens
// ```
