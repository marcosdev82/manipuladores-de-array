// Boa! 🚀 Aqui vão **mais 5 exercícios diferentes**, trabalhando **if, truthy/falsy, if encadeado e precedência de operadores**:

// ---

// ### **Exercício 6 – Login simples**

// Crie uma função `login(username, password)` que:

// * Se `username` ou `password` forem **falsy**, imprima `"Preencha todos os campos"`.
// * Se `username === "admin"` **e** `password === "1234"`, imprima `"Login bem-sucedido"`.
// * Caso contrário, imprima `"Usuário ou senha incorretos"`.

// ---

// ### **Exercício 7 – Par ou Ímpar**

// Crie uma função `checkNumber(num)` que:

// * Se `num` for falsy (`0`, `null`, `NaN`, `undefined`), imprima `"Número inválido"`.
// * Se `num % 2 === 0`, imprima `"Número par"`.
// * Caso contrário, imprima `"Número ímpar"`.

// ---

// ### **Exercício 8 – Desconto em produto**

// Crie uma função `calculateDiscount(price, coupon)` que:

// * Se `price` for falsy, imprima `"Preço inválido"`.
// * Se `coupon` for truthy **e** `price > 100`, aplique 20% de desconto.
// * Se `coupon` for truthy **e** `price <= 100`, aplique 10% de desconto.
// * Se `coupon` for falsy, não aplique desconto.
// * Imprima o valor final.

// ---

// ### **Exercício 9 – Jogo de pontos**

// Crie uma função `gamePoints(points)` que:

// * Se `points` for falsy, imprima `"Sem pontos"`.
// * Se `points >= 1000`, imprima `"Nível Lendário"`.
// * Se `points >= 500`, imprima `"Nível Avançado"`.
// * Se `points >= 100`, imprima `"Nível Intermediário"`.
// * Caso contrário, imprima `"Nível Iniciante"`.

// ---

// ### **Exercício 10 – Checagem de dia da semana**

// Crie uma função `dayCheck(day)` que:

// * Se `day` for falsy, imprima `"Dia inválido"`.
// * Se `day === "sábado"` ou `day === "domingo"`, imprima `"Fim de semana"`.
// * Caso contrário, imprima `"Dia útil"`.
